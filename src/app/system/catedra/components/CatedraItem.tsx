{/*import Link from "next/link";
import { catedraAttributes } from "@/types";
import RouterLinks from "@/config/RouterLinks";

interface props {
	data: catedraAttributes;
}

export const CatedraItem = (props: props) => {
	const { data } = props;
	const { _id, name } = data;

	return (
		<Link href={RouterLinks.catedra.one(_id)}>
			<div>{name}</div>
		</Link>
	);
};*/}
